name: panel.layouts.fullwidth.name
label: panel.layouts.label
icon: bars
preview: fields
wysiwyg: true
tabs:
  content:
    label: panel.tabs.content.label
    type: fields
    fields:
      blocks:
        extends: fields/blocks
        label: " "
        fieldsets:
          matomo: fieldsets/matomo
  style: layouts/tabs/style
  alignment: layouts/tabs/alignment
