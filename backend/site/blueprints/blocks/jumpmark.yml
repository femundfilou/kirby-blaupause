name: panel.blocks.jumpmark.name
icon: anchor
fields:
  jumpmark:
    label: panel.blocks.jumpmark.fields.jumpmark.label
    type: slug
    before: '#'
    required: true
    help: panel.blocks.jumpmark.fields.jumpmark.help
