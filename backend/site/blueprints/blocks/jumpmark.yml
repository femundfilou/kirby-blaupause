name: panel.blocks.jumpmark.name
icon: anchor
fields:
  id:
    label: panel.blocks.jumpmark.fields.id.label
    type: slug
    before: '#'
    required: true
    help: panel.blocks.jumpmark.fields.id.help
